%header.header{ role: 'banner' }
  %nav.top-bar
    %ul.title-area
      %li.name
        = link_to root_path, class: ['brand', 'readable-shadow'] do
          = image_tag('logo_small.png', alt: 'small logo')
          = I18n::t(:base_title)
    %section.top-bar-section
      %ul.right
        %li
          = link_to('Users', users_path, class: 'readable-shadow')
        %li
          = link_to('Events', events_path, class: 'readable-shadow')
